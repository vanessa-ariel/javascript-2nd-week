//const loginButton = document.querySelector(".login__button");
//const input = document.getElementById("loginInputEmail");
 //.login__button is a string

 //loginButton.addEventListener("click", function(event){ //parameters do not receive quotation marks
    //event.preventDefault();
    //const email = input.value;
    //const loginForm = document.querySelector(".login__form");
    //loginForm.innerHTML = `You have successfully logged in as ${email}!`
    //alert(`You have successfully logged in as ${email}!`);
//})



/* ITERATIONS

Inform that I am using a HTML element with document.querySelector

Create a variable for this element and assign a name and a string for the query selector that contains a class ex. (".class_name")

Sign that something needs to be done on login Button when user clicks on it using the method .addEventListener. The first argument is "click" by default, the second one is a function

*/
